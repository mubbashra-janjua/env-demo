{
  "name": "github-pages-deployment",
  "version": "1.0.0",
  "scripts": { 
    {"build": "vue-cli-service build" 
  }
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}

